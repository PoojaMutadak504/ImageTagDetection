kind: pipeline
type: docker
name: default

workspace:
  path: /home/blazemeter
  
platform:
  os: linux
  arch: amd64
  
  
steps:
- name: run-test
  pull: always
  build:
  args:
      APIKEY: boo
      APIKEY: bar
  image: poojamutadak/blazemeter 

