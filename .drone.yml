kind: pipeline
type: docker
name: default

workspace:
  path: /home/blazemeter
  
platform:
  os: linux
  arch: amd64
  
  
steps:
- name: run-test
  pull: always
  build:
  image: poojamutadak/blazemeter 
  build_args:
      from_secret: APIKEY


